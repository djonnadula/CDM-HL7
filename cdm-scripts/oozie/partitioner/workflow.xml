<workflow-app name="${WF_NAME}" xmlns="uri:oozie:workflow:0.5">
    <start to="addPartition"/>
    <action name="Kill">
        <email xmlns="uri:oozie:email-action:0.2">
            <to>CorpISDLCDM-EDW@HCAHealthcare.com</to>
            <subject>${wf:name()} Job Failure</subject>
            <body>The ${wf:name()} Oozie job has failed.
                    JobId: ${wf:id()}
                    JobPath: ${wf:appPath()}
                    ErrorMessage: ${wf:errorMessage(wf:lastErrorNode())}
            </body>
        </email>
        <ok to="Kill-kill"/>
        <error to="Kill-kill"/>
    </action>
    <kill name="Kill-kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <action name="addPartition">
        <hive xmlns="uri:oozie:hive-action:0.2">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <job-xml>${nameNode}/user/dof7475/partitioner/lib/hive-config.xml</job-xml>
            <script>${SCRIPT_PATH}</script>
            <param>MESSAGE_TYPE=${MESSAGE_TYPE}</param>
            <param>MESSAGE_DATE=${MESSAGE_DATE}</param>
        </hive>
        <ok to="End"/>
        <error to="Kill"/>
    </action>
    <end name="End"/>
</workflow-app>