<coordinator-app end="${endTime}" frequency="${coord:hours(4)}" name="${COORD_NAME}" start="${startTime}" timezone="America/Chicago" xmlns="uri:oozie:coordinator:0.1">
   <action>
      <workflow>
         <app-path>${workflowPath}</app-path>
      </workflow>
   </action>
</coordinator-app>