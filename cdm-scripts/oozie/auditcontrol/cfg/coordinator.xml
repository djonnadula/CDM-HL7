<coordinator-app end="${endTime}" frequency="${coord:hours(4)}" name="cmd-bd-${COORD_NAME}" start="${startTime}" timezone="America/Chicago" xmlns="uri:oozie:coordinator:0.1">
   <action>
      <workflow>
         <app-path>${workflowPath}</app-path>
         <configuration>
            <property>
               <name>AuditStartDate</name>
               <value>${coord:actualTime()}</value>
            </property>
         </configuration>
      </workflow>
   </action>
</coordinator-app>